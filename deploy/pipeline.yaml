apiVersion: deploy.cloud.google.com/v1
kind: DeliveryPipeline
metadata:
  name: salam-cicd-pipeline
  labels:
    app: cicd
description: cicd delivery pipeline
serialPipeline:
  stages:
  - targetId: uat
    # profiles:
    # - dev
  # - targetId: staging
  #   profiles:
  #   - staging
  - targetId: prod
  #   profiles:
  #   - prod
